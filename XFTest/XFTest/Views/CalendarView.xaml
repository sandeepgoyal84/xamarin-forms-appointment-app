<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:fftransformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations"
             xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms" 
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView" 
             mc:Ignorable="d"
             x:Name="this"
             x:Class="XFTest.Views.CalendarView">
    <ContentView.Content>
        <StackLayout BindingContext="{x:Reference this}">
            <Grid Padding="0" BackgroundColor="#25A87B" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*" />
                </Grid.ColumnDefinitions>

                <StackLayout Grid.Row="0" Grid.Column="0" Padding="20,20,20,20" >

                    <Grid Padding="0,0,0,10" >

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*" />
                            <ColumnDefinition Width="0.5*" />
                            <ColumnDefinition Width="0.5*" />
                        </Grid.ColumnDefinitions>

                        <Label x:Name="lblMonthYear" Grid.Row="0" Grid.Column="0" Margin="{OnPlatform iOS='0,55,0,0', Android='0,30,0,0'}" Text=""
                               Style="{StaticResource PageHeading}" 
                               HorizontalTextAlignment="Start" 
                               FontFamily="{StaticResource BoldFont}"  FontSize="{StaticResource FontSizeA20I23}" TextColor="#FFFFFF" />
                        <ffimageloadingsvg:SvgCachedImage Margin="{OnPlatform iOS='0,60,0,0', Android='0,30,0,0'}" 
                                Source="Arrow_Left.svg" 
                                WidthRequest="12" 
                                Grid.Row="0" Grid.Column="1"
                                HorizontalOptions="End">
                            <ffimageloadingsvg:SvgCachedImage.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding PrevMonthCommand}" NumberOfTapsRequired="1" />
                            </ffimageloadingsvg:SvgCachedImage.GestureRecognizers>
                        </ffimageloadingsvg:SvgCachedImage>

                        <ffimageloadingsvg:SvgCachedImage Margin="{OnPlatform iOS='0,60,0,0', Android='0,30,0,0'}" 
                                Source="Arrow_Right.svg" 
                                WidthRequest="12" 
                                Grid.Row="0" Grid.Column="2"
                                HorizontalOptions="End">

                            <ffimageloadingsvg:SvgCachedImage.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding NextMonthCommand}" NumberOfTapsRequired="1" />
                            </ffimageloadingsvg:SvgCachedImage.GestureRecognizers>
                        </ffimageloadingsvg:SvgCachedImage>

                    </Grid>


                    <ScrollView BackgroundColor="#25A87B" Orientation="Horizontal" Padding="0, 0, 0, 0" >
                        <StackLayout Orientation="Horizontal" >
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[0].IsVisible}">
                                <Grid x:Name="calPad01" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl01" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[0].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[0].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[0].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl01" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[0].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[0].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[0].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[1].IsVisible}">
                                <Grid x:Name="calPad02" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl02" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[1].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[1].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[1].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl02" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[1].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[1].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[1].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[2].IsVisible}">
                                <Grid x:Name="calPad03" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl03" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[2].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[2].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[2].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl03" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[2].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[2].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[2].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[3].IsVisible}">
                                <Grid x:Name="calPad04" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl04" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[3].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[3].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[3].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl04" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[3].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[3].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[3].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[4].IsVisible}">
                                <Grid x:Name="calPad05" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl05" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[4].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[4].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[4].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl05" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[4].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[4].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[4].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[5].IsVisible}">
                                <Grid x:Name="calPad06" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl06" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[5].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[5].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[5].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl06" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[5].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[5].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[5].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[6].IsVisible}">
                                <Grid x:Name="calPad07" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl07" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[6].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[6].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[6].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl07" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[6].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[6].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[6].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[7].IsVisible}">
                                <Grid x:Name="calPad08" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl08" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[7].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[7].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[7].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl08" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[7].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[7].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[7].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[8].IsVisible}">
                                <Grid x:Name="calPad09" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl09" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[8].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[8].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[8].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl09" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[8].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[8].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[8].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[9].IsVisible}">
                                <Grid x:Name="calPad10" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl10" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[9].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[9].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[9].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl10" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[9].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[9].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[9].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[10].IsVisible}">
                                <Grid x:Name="calPad11" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl11" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[10].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[10].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[10].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl11" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[10].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[10].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[10].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[11].IsVisible}">
                                <Grid x:Name="calPad12" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl12" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[11].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[11].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[11].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl12" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[11].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[11].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[11].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[12].IsVisible}">
                                <Grid x:Name="calPad13" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl13" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[12].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[12].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[12].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl13" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[12].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[12].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[12].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[13].IsVisible}">
                                <Grid x:Name="calPad14" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl14" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[13].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[13].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[13].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl14" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[13].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[13].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[13].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[14].IsVisible}">
                                <Grid x:Name="calPad15" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl15" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[14].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[14].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[14].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl15" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[14].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[14].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[14].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[15].IsVisible}">
                                <Grid x:Name="calPad16" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl16" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[15].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[15].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[15].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl16" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[15].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[15].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[15].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[16].IsVisible}">
                                <Grid x:Name="calPad17" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl17" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[16].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[16].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[16].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl17" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[16].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[16].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[16].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[17].IsVisible}">
                                <Grid x:Name="calPad18" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl18" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[17].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[17].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[17].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl18" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[17].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[17].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[17].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[18].IsVisible}">
                                <Grid x:Name="calPad19" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl19" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[18].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[18].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[18].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl19" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[18].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[18].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[18].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[19].IsVisible}">
                                <Grid x:Name="calPad20" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl20" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[19].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[19].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[19].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl20" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[19].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[19].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[19].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[20].IsVisible}">
                                <Grid x:Name="calPad21" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl21" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[20].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[20].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[20].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl21" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[20].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[20].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[20].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[21].IsVisible}">
                                <Grid x:Name="calPad22" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl22" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[21].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[21].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[21].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl22" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[21].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[21].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[21].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[22].IsVisible}">
                                <Grid x:Name="calPad23" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl23" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[22].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[22].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[22].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl23" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[22].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[22].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[22].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[23].IsVisible}">
                                <Grid x:Name="calPad24" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl24" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[23].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[23].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[23].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl24" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[23].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[23].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[23].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[24].IsVisible}">
                                <Grid x:Name="calPad25" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl25" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[24].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[24].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[24].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl25" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[24].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[24].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[24].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[25].IsVisible}">
                                <Grid x:Name="calPad26" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl26" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[25].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[25].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[25].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl26" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[25].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[25].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[25].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[26].IsVisible}">
                                <Grid x:Name="calPad27" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl27" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[26].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[26].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[26].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl27" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[26].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[26].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[26].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[27].IsVisible}">
                                <Grid x:Name="calPad28" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl28" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[27].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[27].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[27].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl28" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[27].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[27].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[27].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[28].IsVisible}">
                                <Grid x:Name="calPad29" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl29" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[28].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[28].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[28].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl29" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[28].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[28].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[28].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[29].IsVisible}">
                                <Grid x:Name="calPad30" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl30" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[29].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[29].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[29].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl30" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[29].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[29].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[29].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>
                            <StackLayout HorizontalOptions="StartAndExpand" IsVisible="{Binding DayCollection[30].IsVisible}">
                                <Grid x:Name="calPad31" Style="{StaticResource CalDatePad}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                    </Grid.ColumnDefinitions>
                                    <yummy:PancakeView  
            Grid.Row="0" Grid.Column="0">
                                        <Label 
                x:Name="dateLbl31" FontFamily="{StaticResource BoldFont}"
                Style="{StaticResource CalDateText}" Text="{Binding DayCollection[30].Date,StringFormat='{0:D2}'}"   
                TextColor="{StaticResource ColorBluishGrey}" 
                HorizontalOptions="CenterAndExpand"
                HeightRequest="35"
                Padding="4,10,4,0">
                                            <Label.Triggers>
                                                <DataTrigger TargetType="Label" Binding="{Binding DayCollection[30].IsSelected}" Value="True">
                                                    <Setter Property="Padding" Value="10,10,10,10" />
                                                </DataTrigger>
                                            </Label.Triggers>
                                        </Label>
                                        <yummy:PancakeView.Triggers>
                                            <DataTrigger TargetType="yummy:PancakeView" Binding="{Binding DayCollection[30].IsSelected}" Value="True">
                                                <Setter Property="CornerRadius" Value="{OnPlatform iOS='20', Android='45,45,45,45'}" />
                                                <Setter Property="BackgroundColor" Value="#368268" />
                                            </DataTrigger>
                                        </yummy:PancakeView.Triggers>
                                    </yummy:PancakeView>
                                    <Label 
            Grid.Row="1" Grid.Column="0"
            x:Name="dayLbl31" 
            FontFamily="{StaticResource RegularFont}"
            Style="{StaticResource NormalText11Cal}" 
            TextColor="{StaticResource ColorBluishGrey}" 
            HorizontalOptions="CenterAndExpand"
            Text="{Binding DayCollection[30].Day}">
                                        <Label.Triggers>
                                            <DataTrigger TargetType="Label" Binding="{Binding DayCollection[30].IsSelected}" Value="True">
                                                <Setter Property="Padding" Value="0,5,0,0" />
                                                <Setter Property="TextColor" Value="{StaticResource ColorWhite}" />
                                            </DataTrigger>
                                        </Label.Triggers>
                                    </Label>
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding DateSelectCommand}" CommandParameter="{Binding DayCollection[30].Date}" NumberOfTapsRequired="1" />
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </StackLayout>

                        </StackLayout>
                    </ScrollView>

                </StackLayout>

            </Grid>

        </StackLayout>
    </ContentView.Content>
</ContentView>